<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
</head>
<body>
    <h1>Courses</h1>
    {% for item in cour %}
        <p>{{item.title}}</p>
        <p>{{item.content}}</p>
        <a href="/single-course/{{item.id}}">See more</a>
    {% endfor %}
</body>
</html>